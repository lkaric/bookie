# POSTGRES
POSTGRES_USER=unicorn
POSTGRES_PASSWORD=topsecret
POSTGRES_PORT=5432
POSTGRES_DB=bookie
POSTGRES_HOST=localhost

# PRISMA
PRISMA_URL="postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public"